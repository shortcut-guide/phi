---
import EmailPasswordForm from "@/f/components/EmailPasswordForm.astro";
import { getLang } from "@/f/utils/lang"; // 実装済みの共通lang取得ユーティリティ

const res = await fetch("/api/account/email");
const { email } = await res.json();
const lang = getLang(); // configやセッションから取得
---

<EmailPasswordForm />
