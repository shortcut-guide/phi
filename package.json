{
  "scripts": {
    "frontend": "cd frontend && npm start",
    "backend": "cd backend && npm run node:start && npm run d1:start",
    "start": "npm-run-all --parallel frontend backend"
  },
  "dependencies": {
    "@headlessui/react": "^2.2.4"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
